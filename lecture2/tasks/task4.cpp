#include <iostream>
#include <set>
#include <vector>
#include <ranges>
#include <algorithm>

auto suffix(std::set<int>&, std::vector<int>) -> bool;

auto main() -> int {

}

auto suffix(std::set<int>& s, std::vector<int> vec) -> bool {

    std::ranges::sort(vec);




}
